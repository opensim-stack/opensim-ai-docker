# Console2MCP MCP Server

## User Management

 * When creating users, all user fields must be filled out. If any field is left blank, the console will prompt for missing values (First name, Last name, Password, Email, UUID). A unique UUID field  shouldmust be generated as the final argument.