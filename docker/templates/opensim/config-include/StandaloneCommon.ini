[DatabaseService]
    StorageProvider = "OpenSim.Data.MySQL.dll"
    ConnectionString = "Data Source=${MARIADB_HOST};Database=${MARIADB_DATABASE};User ID=${MARIADB_USER};Password=${MARIADB_PASSWORD};Old Guids=true;SslMode=None;"

[Hypergrid]
    HomeURI = "${Const|BaseURL}:${Const|PublicPort}"

[Modules]
    AssetCaching = "FlotsamAssetCache"
    Include-FlotsamCache = "config-include/FlotsamCache.ini"

[AssetService]
    DefaultAssetLoader = "OpenSim.Framework.AssetLoader.Filesystem.dll"
    AssetLoaderArgs = "assets/AssetSets.xml"

[GridService]
    StorageProvider = "OpenSim.Data.Null.dll:NullRegionData"
    Region_${OPENSIM_REGION_NAME_SAFE} = "DefaultRegion, DefaultHGRegion"
    ExportSupported = true

[LoginService]
    WelcomeMessage = "${OPENSIM_WELCOME_MESSAGE}"

    SRV_HomeURI            = "${Const|BaseURL}:${Const|PublicPort}"
    SRV_InventoryServerURI = "${Const|BaseURL}:${Const|PublicPort}"
    SRV_AssetServerURI     = "${Const|BaseURL}:${Const|PublicPort}"
    SRV_ProfileServerURI   = "${Const|BaseURL}:${Const|PublicPort}"
    SRV_FriendsServerURI   = "${Const|BaseURL}:${Const|PublicPort}"
    SRV_IMServerURI        = "${Const|BaseURL}:${Const|PublicPort}"
    MapTileURL             = "${Const|BaseURL}:${Const|PublicPort}/"

[GridInfoService]
    login = ${Const|BaseURL}:${Const|PublicPort}/
    gridname = "${OPENSIM_GRID_NAME}"
    gridnick = "${OPENSIM_GRID_NICK}"

[MapImageService]
    TilesStoragePath = "maptiles"
