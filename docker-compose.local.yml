services:
    piper:
        build:
            context: ../opensim-piper
        image: "${OPENSIM_PIPER_IMAGE:-opensim-piper:latest}"
    
    spawner:
        build:
            context: ../opensim-spawner
        image: "${OPENSIM_SPAWNER_IMAGE:-opensim-spawner:latest}"
